"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.searchPersonByName = void 0;
const IMDBPersonSearchResolver_1 = require("./resolvers/IMDBPersonSearchResolver");
const enums_1 = require("./enums");
async function searchPersonByName(queryName, { exactMatch = false, sourceType = enums_1.Source.IMDB, } = {}) {
    //  select the resolver
    let resolver;
    switch (sourceType) {
        case enums_1.Source.IMDB:
        default:
            resolver = new IMDBPersonSearchResolver_1.IMDBPersonSearchResolver(queryName, {
                exactMatch,
            });
    }
    // get details from resolver
    return await resolver.getResult();
}
exports.searchPersonByName = searchPersonByName;
